
import 'package:flutter/material.dart';
import 'package:todo_v2/bloc/themes.dart';

var dark = DemoTheme(
    'dark',
    ThemeData.dark()
);
var light = DemoTheme(
    'light',
    ThemeData.light()
);